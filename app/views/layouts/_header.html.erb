 <header>
     <nav>
            <%= link_to image_tag('logo.jpg'), root_path %>
            <ul class="nav-list">
            <% if !current_user %>
              <li>
                <%= link_to 'All Movies', movies_path %>
              </li>
              <li>
                <%= link_to 'Add New Movie', new_movie_path %>
              </li>
                <li>
                <%= link_to 'All Users', users_path %>
              </li>
                <li>
                <%= link_to 'Sign In', new_session_path %>
              </li>
                <li>
                <!-- <%= link_to 'Sign Up', new_user_path %> -->
                <%= link_to 'Sign Up', signup_path %>
              </li>
            <% else %>
<!-- <li>
  <%= link_to 'Sign Out', session_path, method: :delete, data: { turbo_method: :delete } %>
</li> -->

<li>
  <%= form_with url: session_path, method: :delete, local: true do %>
    <%= submit_tag 'Sign Out', class: "sign_out" %>
  <% end %>
</li>





                 <li>
                <!-- this defines the current user 
                User.find(session[:user_id]).name
                but we are going to create an helper method for it -->
                <strong><%= link_to current_user.name, current_user %></strong>
              </li>
              <% end %>
            </ul>
          </nav>
 </header>